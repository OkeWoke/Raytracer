/*#include "GObjects/Mesh.h"
#include "Vector.h"
#include <vector>
#include "TreeNode.h"
#ifndef OCTTREE_H
#define OCTTREE_H

class OctTree
{
    public:
        OctTree();
        OctTree(std::vector<Mesh*>& meshes, int depth);

    private:
        int max_depth;
        TreeNode* root_node;
        std::vector<Mesh*> meshes;
};

#endif // OCTTREE_H

*/
